#!/usr/bin/env bash

# - ESS_MASTER_MEMORY, to set how much total memory of ess master (e.g. 1000m, 2g)
# - ESS_WORKER_MEMORY, to set how much total memory of ess workers (e.g. 1000m, 2g)
# - ESS_WORKER_OFFHEAP_MEMORY, to set how much total off-heap memory of ess workers (e.g. 1000m, 2g)
# - ESS_MASTER_JAVA_OPTS
# - ESS_WORKER_JAVA_OPTS
# - ESS_PID_DIR
# - ESS_LOG_DIR